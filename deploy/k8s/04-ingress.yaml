apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rwatch-web
  namespace: rwatch-web
  labels:
    app: rwatch-web
  annotations:
    argocd.argoproj.io/sync-wave: "4"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - telemetry.jakob-lingel.dev
      secretName: rwatch-web-tls
  rules:
    - host: telemetry.jakob-lingel.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rwatch-web
                port:
                  number: 80
